# =============================================================================
# FRONTEND — production (cabinet.wilaya-tlemcen.dz)
# =============================================================================
# Relative /api = same origin as the site (recommended on cPanel).
# Build: npm run build  →  upload dist/ to public_html
# =============================================================================

VITE_API_URL=/api

# Alternative (absolute URL):
# VITE_API_URL=https://cabinet.wilaya-tlemcen.dz/api
